export interface Listing {
  id: number;
  user_id: string;
  title: string;
  quantity: number;
  description: string;
  location: string;
  available_until: string;
  created_at: string;
  contact_info: string;
}
